class Settings {
    // Canvas dimensions
    static canvasW = 1000;
    static canvasH = 5000;

    // Font/Text settings/vars
    static point      = 25;
    static _point     = 20;
    static textWeight =  2;
    static rectWeight =  5;
    static font;

    // Offset used for spacing and buffering
    static offset = 25;
    
    // Universe Colors
    static UNI_COLORS = {
        'lib': '#F5D327',
        'lev': '#27B0F5',
        'spn': '#C223BD',
        'psy': '#23C227',
        'men': '#C22823'
    }

    // General colors
    static black    = '#000000';
    static white    = '#ffffff';
    static grey     = '#646464';
    static darkGrey = '#1b1b1b';
    
    // Auto scroll setting
    static scroll = false;
    
    // Sort options
    static SORT = {
        DATE: 'date',
        UNIVERSE: 'universe'
    }
}